import 'package:flutter/cupertino.dart';

class Theme {
  FontStyle? font;
  Color? fontColor;
  Color? backgroundColor;
  FontWeight? fontWeight;
  Theme(this.font, this.fontColor, this.fontWeight, this.backgroundColor);
}
